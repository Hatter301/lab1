#include <iostream>
#include <cmath>
using namespace std;

int main()
{
   double a,b,s,r,p,c;
   cin>>a>>b;
   s=a*a+b*b;
   r=a*a-b*b;
   p=a*a*b*b;
   c=(a*a)/(b*b);
   cout<<s<<" "<<r<<" "<<p<<" "<<c;

    return 0;
}
