#include <iostream>
#include <cmath>
using namespace std;

int main()
{
    double x1,x2,x,a,b,c;
    cin>>a>>b>>c;
    if (a!=0) {
       x1=(b+sqrt(b*b-4*a*c))/2*a;
       x2=(b-sqrt(b*b-4*a*c))/2*a;
       cout<<x1<<" "<<x2;
    } else {
        x=-c/b;
        cout<<x;
    }
    return 0;
}
